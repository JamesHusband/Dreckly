import { ClientLayout } from './ClientLayout';
import { render } from '@testing-library/react';

describe('ClientLayout', () => {
  it('should render', () => {
    render(<ClientLayout>Hello</ClientLayout>);
  });
});
